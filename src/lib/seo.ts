/**
 * Thin, boilerplate legal pages we keep out of search results (noindex + excluded
 * from the sitemap). They remain public and linked in the footer — this is SEO
 * hygiene, not a reduction of any legal obligation.
 */
export const NOINDEX_SLUGS = new Set(['privacy', 'cookies', 'terms'])
